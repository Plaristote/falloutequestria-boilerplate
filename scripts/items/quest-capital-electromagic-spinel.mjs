import {ItemBehaviour} from "./item.mjs";

export default class QuestCapitalElectromagicSpinel extends ItemBehaviour {
  constructor(model) {
    super(model);
  }

  // TODO must show different description depending on whether the player
  //      has enough science skill
}
